using System;

namespace FlatPhysics
{
    public class Class1
    {
        FlatVector a = (2, 3);
        FlatVector b = (1, 2);

        FlatVector c = (a.X + b.X, a.Y + b.Y);

        
    }
}